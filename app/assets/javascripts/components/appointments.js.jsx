var Appointments = React.createClass({  getInitialState: function(){    return ({      appointments: this.props.appointments,      title:  this.props.appointments.length,      time: 'Tomorrow at 9am'    })  },  handleUserInput: function(obj){    this.setState(obj)  },  handleFormSubmit: function(){    var appointment = { title: this.state.title, time: this.state.time };    var that = this;    $.post('/appointments', {appointment: appointment}, function(result){      that.setState({appointments: result})    }, 'json')  },  handleAppointmentDelete: function(appointment_id){    var that = this;    $.ajax({      url: '/appointments/' + appointment_id,      type: 'DELETE',      format: 'json',      success: function(result) {        that.setState({appointments: result})      }    });  },  render: function() {    return (      <div>        <AppointmentForm title={this.state.title}                         time={this.state.time}                         onUserInput={this.handleUserInput}                         onFormSubmit={this.handleFormSubmit} />        <AppointmentsList appointments={this.state.appointments} onAppointmentDelete={this.handleAppointmentDelete} />      </div>    )  }});